apiVersion: apps/v1
kind            - name: REACT_APP_API_URL
              value: "https://runstack.pp.ua/api"Deployment
metadata:
  name: frontend
  labels:
    environment: production
spec:
  replicas: 2  # More replicas for production
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
        environment: production
    spec:
      containers:
        - name: frontend
          image: developmentbranch.azurecr.io/frontend-prod:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          env:
            - name: REACT_APP_BASE_URL
              value: "https://runstack.pp.ua/api"
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
